[build-system]
requires = ["setuptools>=45"]
build-backend = "setuptools.build_meta"

[project]
name = "aether-summarizer"
version = "0.1.0"
description = "Modular PDF text extraction and LLM summarization engine"